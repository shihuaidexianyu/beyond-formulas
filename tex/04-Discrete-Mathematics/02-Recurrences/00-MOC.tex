递推式说明规模 \(n\) 的对象怎样由较小规模生成，通常不会直接给出第 \(n\) 项的闭式表达。它适合描述算法运行时间、组合对象数量和动态过程。分析时先保证初值足以唯一确定序列，再根据结构选择展开、递归树、特征方程或生成函数。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  递推建模与递归树
\item
  线性递推与特征根
\item
  生成函数把递推变成代数
\end{enumerate}

闭式公式不是唯一目标。有时渐近增长、稳定性或可计算递推已经更有价值；方法应服务于问题，而不是强迫每个递推都写成复杂根式。
