给定有限样本，我们想在未观测位置重建函数。插值要求曲线逐点命中数据，逼近则允许偏离样本以换取整体误差更小或模型更简单。高次多项式虽然自由度多，却可能因节点几何在端点剧烈振荡；样条通过分段低次结构获得局部性。这个单元关注的不是公式数量，而是数据、函数空间、误差范数与节点选择怎样共同决定结果。

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  一个插值多项式的两种构造
\item
  Runge 现象与节点选择
\item
  从精确插值到整体逼近
\item
  三次样条：用局部性换稳定
\end{enumerate}
