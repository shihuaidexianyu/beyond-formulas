\section{换元与分部积分：反向识别求导规则}
\label{sec:02-Calculus/04-Integration/03}

一台设备的功率曲线 \(P(t)\) 记在以秒为横轴的图上，单位是瓦特（焦耳/秒）。曲线下的几何面积，代表这段时间内设备所做的总功（焦耳）：

\[
W = \int P(t)\,dt.
\]

现在我们要把时间单位换成 ``分钟''，记 \(m\) 为分钟数（即 \(t = 60m\)）。

如果在坐标图上直接把横轴刻度换成 ``分钟''，原先跨度 60 秒的一段时间，在新的横轴上长度变成了 1 分钟。整张图在水平方向上被压缩到了原来的 60 分之一，曲线下的纯几何面积 \(\int P(60m)\,dm\) 自然也缩到了原来的 60 分之一。

但设备实际做的物理功不可能因为我们换了个时间单位就凭空减少。问题出在哪里？

在新的分钟坐标下，横轴上移动一个小单位 \(dm\)（1 分钟），实际上代表时间过去了 60 秒。为了还原真实的物理量，每一小段微元宽度 \(dm\) 必须乘上转换因子 60——也就是 \(dt = 60\,dm\)。真实的总功应该是：

\[
W = \int P(60m) \cdot (60\,dm).
\]

积分号里那个 \(dx\)（或 \(dt\)）绝对不是用来标明 ``积分变量是谁'' 的装饰品，它本身就带着 ``一小段实际宽度'' 的量纲与尺度。换坐标时，被积函数和微元宽度必须一起变换。

本节的两件工具都从这一点出发：换元管的是坐标伸缩带来的倍率，分部管的是导数在两个因子之间搬家。

\subsection{换元：坐标伸缩带来的因子}

换元公式看似是一套代数替换技巧，实质上是\textbf{链式法则的逆向应用}。

设 \(F(x)\) 是 \(f(x)\) 的一个原函数（即 \(F'(x) = f(x)\)）。根据求导的链式法则，复合函数 \(F(g(t))\) 关于 \(t\) 的导数为：


\[
\frac{d}{dt} F(g(t)) = F'(g(t)) \cdot g'(t) = f(g(t)) \, g'(t).
\]

两边在区间 \(t \in [a, b]\) 上求积分。由微积分基本定理，左边 ``先导再积'' 的结果直接等于端点值做差：


\[
\int_a^b f(g(t)) \, g'(t) \, dt = \int_a^b \frac{d}{dt}F(g(t)) \, dt = \Bigl[ F(g(t)) \Bigr]_a^b = F(g(b)) - F(g(a)).
\]

而看右边的定积分 \(\int_{g(a)}^{g(b)} f(x) \, dx\)，按微积分基本定理计算，其结果同样是 \(F(g(b)) - F(g(a))\)。两边指向同一个数值，由此得到了\textbf{换元积分公式}：


\[
\int_{g(a)}^{g(b)} f(x) \, dx = \int_a^b f(g(t)) \, g'(t) \, dt.
\]

从几何与微元的视角来看，这个公式是在做一次\textbf{坐标轴的重新标定}（设 \(x = g(t)\)）：

1. \textbf{高度对应}：在 \(x\) 坐标下的函数高度 \(f(x)\)，翻译到 \(t\) 坐标下就是 \(f(g(t))\)。
2. \textbf{宽度伸缩}：当 \(t\) 改变微小量 \(dt\) 时，\(x\) 的改变量并非 \(dt\)，而是 \(dx \approx g'(t) \, dt\)。这里的导数 \(g'(t) = \frac{dx}{dt}\) 扮演了\textbf{局部伸缩率}的角色——如果某点处 \(g'(t) = 2\)，意味着在该点附近，\(x\) 坐标轴相比 \(t\) 坐标轴被拉伸到了 2 倍宽。
3. \textbf{范围同步}：当 \(t\) 从起点 \(a\) 走到终点 \(b\) 时，\(x\) 恰好从起点 \(g(a)\) 运动到终点 \(g(b)\)。

两边算的是同一块累积总量：右边是在 \(t\) 的坐标里数小格子，每一格宽 \(dt\)、高 \(f(g(t)) \cdot g'(t)\)；左边是在 \(x\) 的坐标里数小格子，每一格宽 \(dx\)、高 \(f(x)\)。\(g'(t)\) 就是维持这两套记账方式平衡的补足因子。

\begin{center}
\begin{tikzpicture}[x=1.1cm,y=1.0cm,font=\small,>=stealth]
% --- 左图：x 空间 ---
\begin{scope}[shift={(0,0)}]
\draw[->] (-0.2,0) -- (3.4,0) node[right] {\(x\)};
\draw[->] (0,-0.2) -- (0,2.6) node[above] {\(y\)};
\draw[blue!70!black,thick] plot[domain=0.4:3.0,samples=50] (\x,{1.75 - 0.18*(\x-1.5)*(\x-1.5)});
\node[blue!70!black,font=\footnotesize,fill=white,inner sep=1pt] at (2.35,1.68) {\(y = f(x)\)};
% 矩形
\fill[blue!15,draw=blue!60,thick] (1.45,0) rectangle (1.75,1.748);
\node[below,font=\footnotesize] at (1.6,0) {\(\Delta x\)};
\node[font=\footnotesize,blue!80!black,fill=white,inner sep=1pt] at (1.6,0.84) {\(f(x)\Delta x\)};

\node[below,font=\footnotesize] at (0.4,-0.05) {\(g(a)\)};
\node[below,font=\footnotesize] at (3.0,-0.05) {\(g(b)\)};

\end{scope}

% --- 中间等效箭头 ---
\draw[<->,thick,black!55] (3.8,1.15) -- node[above,font=\footnotesize,fill=white,inner sep=1pt] {面积等价} node[below,font=\footnotesize,fill=white,inner sep=1pt] {\(\Delta x = g'(t)\Delta t\)} (5.2,1.15);

% --- 右图：t 空间 ---
\begin{scope}[shift={(5.6,0)}]
\draw[->] (-0.2,0) -- (3.4,0) node[right] {\(t\)};
\draw[->] (0,-0.2) -- (0,3.1) node[above] {\(y\)};
\draw[red!70!black,thick] plot[domain=0.3:2.8,samples=50] (\x,{0.35 + 2.0*\x - 0.4*\x*\x});
\node[red!70!black,font=\footnotesize,fill=white,inner sep=1pt] at (1.85,2.82) {\(y = f(g(t))g'(t)\)};
% 矩形
\fill[red!15,draw=red!60,thick] (1.08,0) rectangle (1.22,2.12);
\node[below,font=\footnotesize] at (1.15,0) {\(\Delta t\)};
\node[font=\footnotesize,red!80!black,fill=white,inner sep=1pt] at (1.15,1.08) {\(\text{高} \times \Delta t\)};

\node[below,font=\footnotesize] at (0.3,-0.05) {\(a\)};
\node[below,font=\footnotesize] at (2.8,-0.05) {\(b\)};

\end{scope}
\end{tikzpicture}

\emph{换元前后切片面积的几何等价。在 \(x\) 空间中较宽的矩形（\(\Delta x\) 较大），映射到 \(t\) 空间中宽度缩窄为 \(\Delta t\)，但其高度被局部伸缩率 \(g'(t)\) 相应抬高，使得两套坐标系下每一个微小矩形的面积始终保持相等。}
\end{center}

做一个最短的示范。\(\int_0^{\sqrt\pi} 2x\cos(x^2)\,dx\) 里同时出现了内层 \(x^2\) 和它的导数 \(2x\)。令 \(u=x^2\)，则 \(du=2x\,dx\)，边界同步为 \(u:0\to\pi\)，积分变成 \(\int_0^\pi\cos u\,du=0\)。原来那个看不出所以然的被积函数，换到 \(u\) 的坐标里就是一段标准余弦，正负两半正好抵消。

能这样换的前提是内层和它的导数同时在场，至多差个常数。缺了就动不了：对 \(\int\cos(x^2)\,dx\) 硬设 \(u=x^2\)，手里没有 \(x\,dx\) 可以吸收 \(du\)，换完只会更乱。这不是手法不够，而是这个积分在初等函数里本就没有闭式原函数。

定积分换元还有一个操作上的常见事故：换了变量却沿用旧边界。要么换元后直接用新边界，要么回代原变量再用原边界，两条路都对，中途变道就错。

\subsection{同一个因子的其它三副面孔}

一维的 \(g'(t)\) 看起来只是个乘数，但它是后面好几件事的同一个源头，值得在这里认下来。

概率里做变量替换时，密度必须带着这个因子走。若 \(Y=g(X)\) 且 \(g\) 单调可微，则

\[
p_Y(y)=p_X\bigl(g^{-1}(y)\bigr)\cdot\left\lvert\frac{d}{dy}g^{-1}(y)\right\rvert
\]

原因和上图一模一样：概率是密度乘宽度，坐标一拉伸，宽度变了，密度就得反向缩放，才能让每一小块的概率保持不变。绝对值的出现是因为概率不认方向，只认这块区间被拉大了还是压小了。这条式子和它的用法见 \hyperref[sec:05-Probability/03-Random-Variables/04]{``随机变量变换与逆变换抽样''}。

维数升上去，伸缩率就不再是一个数。二维以上的坐标变换在每个方向上的拉伸幅度各不相同，记录它的是 Jacobian 矩阵，而体积的总倍率是它的行列式的绝对值 \(\abs{\det J}\)；\hyperref[sec:02-Calculus/06-Multivariable-Calculus/07]{``多重积分与变量替换''}会把这件事做完。深度生成模型里的 normalizing flow 用一串可逆变换把简单分布推成复杂分布，训练目标里那个 \(\log\abs{\det J}\) 项，来源正是这里——网络设计上之所以偏爱耦合层和自回归结构，就是为了让这个行列式便宜到能算。

所以 ``换元'' 这个名字有点委屈了它。真正在发生的是换坐标，而任何换坐标都要为宽度的变化付一次账。

\subsection{分部：拿一个边界项换一个积分}

有些被积函数根本没有 ``内层''。\(\int x e^x\,dx\) 里的 \(x\) 和 \(e^x\) 是并列的两块，换元无从下手。

这时改读乘积法则。由 \((uv)'=u'v+uv'\) 两边积分并移项，得

\[
\int_a^b u\,dv=\bigl[uv\bigr]_a^b-\int_a^b v\,du
\]

发生的事情是：导数从一个因子身上被挪到了另一个因子身上，搬运费是一个边界项。你赌的是搬完之后剩下的积分更好算。

对 \(\int x e^x\,dx\) 取 \(u=x\)、\(dv=e^x\,dx\)，导数从 \(e^x\) 挪到 \(x\) 上，而 \(x\) 求导后变成 1：

\[
\int x e^x\,dx=xe^x-\int e^x\,dx=e^x(x-1)+C
\]

选 \(u\) 的判据就藏在这句话里：让求导之后更简单的那一块当 \(u\)。多项式求导降次，对数求导变成幂函数，反三角求导变成代数式，它们都是 \(u\) 的好人选；指数与正余弦求导之后原地打转，放进 \(dv\) 最省事。反过来选，比如把 \(dv=x\,dx\)，新积分里会冒出 \(x^2\)，次数不降反升——一步之内就能看出方向错了，回头重选即可。

\(\int x^n e^x\,dx\) 需要连搬 \(n\) 次才把多项式削平，\(\int e^x\cos x\,dx\) 则要搬两次让原积分在等式两边重现，再当作未知数解出来；这些都是同一个搬运动作的重复使用，不必当作独立招式记。

边界项 \(\bigl[uv\bigr]_a^b\) 常被当成累赘，其实它是分部积分在更高阶数学里的主角。在概率上就能看见：设密度 \(p\) 在无穷远处衰减到零，对 \(\int f'(x)p(x)\,dx\) 做分部，边界项消失，剩下

\[
\E\bigl[f'(X)\bigr]=\E\Bigl[f(X)\cdot\bigl(-(\log p)'(X)\bigr)\Bigr]
\]

右边括号里的东西就是 score 函数的相反数。取标准正态时 \(-(\log p)'(x)=x\)，式子塌成

\[
\E\bigl[f'(X)\bigr]=\E\bigl[X f(X)\bigr]
\]

即 Stein 恒等式。它把 ``对函数求导'' 换成了 ``对分布加权''，是 score matching 与扩散模型里反复用到的一步。而边界项在无穷远处消失，在这里是实质条件，不是排版细节。

\subsection{技巧到顶之后往哪走}

换元和分部覆盖面很广，但覆盖不全。并非每个连续函数的原函数都能写成初等函数的有限组合。最出名的例子是 \(e^{-x^2}\)：处处连续，任何有限区间上的定积分都存在且唯一，却没有初等闭式原函数。它随后会以正态密度的身份在概率论里长驻，而正态分布的区间概率历来是靠数值积分或误差函数给出的。

所以要把两种 ``做不出来'' 分开。一种是策略没选对，换个思路还有救；另一种是这个原函数根本不在初等函数的语言里，再换多少次也没用。落到后一种时，出路是数值积分（见 \hyperref[ch:064]{``数值微分与积分：同一份采样，为何难度相反''}），或者在高维情形下把积分写成期望再采样估计——蒙特卡洛方法的动机就是这么朴素：解析地积不出来，那就抽样。

下一节先处理一类结构清楚、总能拆开的被积函数——有理函数，再把积分的边界推到无穷远，看看 ``积分收敛'' 这件事到底由什么决定。