消元能够求解具体的 \(Ax=b\)，但它不断暴露出更一般的结构：齐次解可以相加，矩阵的列组合形成一个封闭集合，函数和多项式也能以同样方式相加、缩放。向量空间把这些表面不同的对象统一起来；基和维数回答``一个空间真正有多少独立方向''；核与像则说明线性变换丢失了什么、能够产生什么。

\subsection{阅读顺序}

\begin{enumerate}
\item
  为什么需要向量空间\\
  从齐次方程的叠加性质出发，理解向量空间、子空间、线性组合与张成。
\item
  基、坐标与维数\\
  在``足够表示''和``没有冗余''之间找到基，并理解坐标依赖于基而维数不依赖于基。
\item
  矩阵的四个基本子空间\\
  把行空间、列空间、零空间和左零空间放回输入---输出图景，用秩---零度定理连接它们的维数。
\item
  线性变换与换基\\
  区分线性变换与它在特定基下的矩阵表示，理解核、像、坐标变换和相似变换。
\end{enumerate}

本单元主要对应 MIT 18.06 中关于向量空间、零空间、基、维数、四个基本子空间和线性变换的讲授。阅读前应熟悉上一单元\hyperref[ch:017]{``线性方程组与矩阵''}中的消元和主元。

\subsection{怎么读这一章}

首次阅读抓住``空间—基—变换''三层：向量空间说明哪些线性组合合法，基把抽象对象编码成坐标，线性变换则描述不依赖坐标的作用。四个基本子空间是矩阵版本的输入、输出与信息丢失，应与秩---零度定理连着读。换基公式的矩阵乘法可以第二遍再熟练，但必须从一开始区分线性变换本身与它在某组基下的表示。

\subsection{本章篇目}

以下篇目按概念依赖排列；若从具体问题进入，也可以先打开最接近当前困惑的一篇，再沿篇内链接回补。

\begin{itemize}
\tightlist
\item \hyperref[sec:03-Linear-Algebra/02-Vector-Spaces/01]{``为什么需要向量空间''}；
\item \hyperref[sec:03-Linear-Algebra/02-Vector-Spaces/02]{``基、坐标与维数：用最少的方向完整描述空间''}；
\item \hyperref[sec:03-Linear-Algebra/02-Vector-Spaces/03]{``矩阵的四个基本子空间：输入、输出与丢失的信息''}；
\item \hyperref[sec:03-Linear-Algebra/02-Vector-Spaces/04]{``线性变换与换基：对象不变，坐标可以改变''}。
\end{itemize}
