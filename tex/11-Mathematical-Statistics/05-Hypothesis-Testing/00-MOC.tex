假设检验要在看数据前规定一条决策规则，并控制它在原假设成立时误报的概率；事后比较``哪个故事更像真的''没有这种错误率保证。

本单元按决策逻辑展开：

\begin{enumerate}
\tightlist
\item
  两类错误与功效共同定义检验区分原假设、备择、显著性水平、第二类错误和功效，并解释样本量与效应量。
\item
  \(p\) 值不是原假设为真的概率从尾概率定义出发，逐一拆解常见误读和可选停止造成的失准。
\item
  似然比比较两个模型对数据的解释力从 Neyman--Pearson 引理走向广义似然比、Wilks 近似及其边界。
\item
  多重比较改变错误的计量单位区分 family-wise error 与 false discovery rate，并把筛选过程纳入推断。
\end{enumerate}

检验输出的是在特定模型、设计和错误控制下的决策证据。它不等于效应大小、实际重要性或因果结论。

\subsection{怎么读这一章}

把检验当成事先设计的决策规则，而不是观测数据后临时寻找一个小数字。先同时看两类错误与功效，再解释 P 值，之后进入似然比和多重比较。每一步都要说明零假设、拒绝规则和错误率究竟针对什么重复过程。

\subsection{本章篇目}

以下篇目按概念依赖排列；若从具体问题进入，也可以先打开最接近当前困惑的一篇，再沿篇内链接回补。

\begin{itemize}
\tightlist
\item \hyperref[sec:11-Mathematical-Statistics/05-Hypothesis-Testing/01]{``两类错误与功效共同定义检验''}；
\item \hyperref[sec:11-Mathematical-Statistics/05-Hypothesis-Testing/02]{``\texorpdfstring{\(p\) 值不是原假设为真的概率}{p 值不是原假设为真的概率}''}；
\item \hyperref[sec:11-Mathematical-Statistics/05-Hypothesis-Testing/03]{``似然比比较两个模型对数据的解释力''}；
\item \hyperref[sec:11-Mathematical-Statistics/05-Hypothesis-Testing/04]{``多重比较改变错误的计量单位''}。
\end{itemize}
