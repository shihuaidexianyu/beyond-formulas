样本协方差矩阵是统计推断的核心对象。低维时（样本量 \(n\) 远大于维数 \(p\)），大数定律保证它收敛到真实协方差；但当 \(p\) 与 \(n\) 同阶增长时，样本协方差的特征值会系统性偏离真实值——即使数据完全干净。随机矩阵理论给出这种偏差的精确形状。

用 Python 或 R 生成一个 \(1000\times1000\) 的标准 Gaussian 数据矩阵，\(p=n=1000\)，真实协方差是 \(I\)（特征值全为 1）。算一下样本协方差矩阵的特征值：最小特征值约 0，最大特征值约 4。全部 1000 个特征值不是紧贴 1，而是弥散在 \([0,4]\) 的区间上形成一个光滑的直方图。增加 \(p\) 和 \(n\) 而保持比例 \(c=p/n=1\) 不变，这个形状不会收敛到一个点——这不是有限样本误差，是高维极限下的确定形状。

本单元回答三个问题：

\begin{enumerate}
\tightlist
\item
  样本协方差在高维为何会失真：\(p/n\to c>0\) 时，样本特征值的分布不再集中于真实特征值附近，而是弥散成一个有宽度的谱。
\item
  Marchenko--Pastur 律给出谱的精确形状：独立同分布数据的样本协方差谱收敛到一个确定的分布，其支撑与形状由 \(c\) 决定。
\item
  谱偏差怎样影响统计推断：主成分分析、协方差收缩、特征值校正各自如何处理这种失真，以及``大样本渐近''与``高维渐近''的差别。
\end{enumerate}

这三个问题共用一条主线：高维统计的困难不只是``参数多''，而是随机矩阵的谱在高维极限下呈现出与低维直觉完全不同的形状。理解这种形状，是设计高维协方差估计与随机投影方法的前提。

\subsection{怎么读这一章}

这一章讨论的是维数与样本量同阶时，经典``大样本、固定维数''直觉为何失效。先观察样本协方差的谱怎样被噪声拉宽，再用 Marchenko--Pastur 律建立基准，最后看这些谱偏差怎样传到 PCA 与随机投影。

\subsection{本章篇目}

以下篇目按概念依赖排列；若从具体问题进入，也可以先打开最接近当前困惑的一篇，再沿篇内链接回补。

\begin{itemize}
\tightlist
\item \hyperref[sec:11-Mathematical-Statistics/09-Random-Matrix-Theory/01]{``样本协方差在高维为何会失真''}；
\item \hyperref[sec:11-Mathematical-Statistics/09-Random-Matrix-Theory/02]{``Marchenko--Pastur 律给出谱的精确形状''}；
\item \hyperref[sec:11-Mathematical-Statistics/09-Random-Matrix-Theory/03]{``谱偏差怎样影响 PCA 与随机投影''}。
\end{itemize}
