复杂统计量的抽样分布可能难以解析，模型复杂度也很难只靠训练误差选择。重采样方法利用已有数据近似``如果重新抽样会怎样''，但每种重排都对应特定假设与目标。

本单元区分四种常被混用的任务：

\begin{enumerate}
\tightlist
\item
  Bootstrap 近似估计量的抽样波动从经验分布重抽样，用于标准误、偏差与区间。
\item
  置换检验依赖可交换性在原假设下重排标签或处理分配，用于构造检验分布。
\item
  交叉验证评估的是完整训练流程说明切分、预处理、调参和测试必须共同设计，避免信息泄漏。
\item
  偏差---方差、正则化与模型选择把预测误差分解、AIC、BIC 与交叉验证放在各自目标下比较。
\end{enumerate}

Bootstrap、permutation 与 cross-validation 都会``重复使用数据''，但它们模拟的随机机制完全不同，不能只因实现都包含循环和随机抽样就互相替代。

\subsection{怎么读这一章}

这四篇的共同问题是：没有新的总体样本时，怎样近似完整分析流程在重复数据上的表现。Bootstrap 重抽观测，置换检验重排标签，交叉验证重跑训练流程；它们成立的交换性与独立性条件并不相同。最后再把这些工具放回偏差—方差与模型选择。

\subsection{本章篇目}

以下篇目按概念依赖排列；若从具体问题进入，也可以先打开最接近当前困惑的一篇，再沿篇内链接回补。

\begin{itemize}
\tightlist
\item \hyperref[sec:11-Mathematical-Statistics/06-Resampling-and-Model-Selection/01]{``Bootstrap 近似估计量的抽样波动''}；
\item \hyperref[sec:11-Mathematical-Statistics/06-Resampling-and-Model-Selection/02]{``置换检验依赖可交换性''}；
\item \hyperref[sec:11-Mathematical-Statistics/06-Resampling-and-Model-Selection/03]{``交叉验证评估的是完整训练流程''}；
\item \hyperref[sec:11-Mathematical-Statistics/06-Resampling-and-Model-Selection/04]{``偏差---方差、正则化与模型选择''}。
\end{itemize}
