熵回答的是``一个分布自身有多不确定''，但许多实际问题比较的是两个分布：数据真正服从 \(P\)，模型却认为它服从 \(Q\)；训练集与测试集是否发生漂移；两种生成机制是否容易区分。只比较参数或概率的逐点差值，往往说不清哪些错误代价最大。

这一单元从一个具体冲突出发：如果数据来自 \(P\)，我们却按照 \(Q\) 分配码长，平均会多付多少 bit？这个问题自然产生交叉熵和 KL 散度，也让 KL 的方向性与支持条件不再只是需要背诵的性质。随后我们证明错误模型不可能在平均意义下胜过真实模型，并讨论当任务需要对称、有界的比较时，为什么会改用 Jensen--Shannon 散度等量。

建议依次阅读：

\begin{enumerate}
\tightlist
\item
  错误模型的代价：从交叉熵到 KL 散度
\item
  Gibbs 不等式与 KL 的方向性
\item
  Jensen--Shannon 散度与尺度选择
\end{enumerate}

读完后，请带走三个判断习惯：先问期望是对哪个分布取的，再检查参考分布是否覆盖真实分布的支持，最后根据任务决定方向性究竟是信息还是麻烦。一张``散度性质表''只能起查阅作用。

\subsection{怎么读这一章}

\begin{itemize}
\tightlist
\item
  第一次读：把 KL 读成``真实数据按错误模型处理时的额外平均代价''，每次看到 \(D(P\|Q)\) 都先说清谁产生数据、谁充当参照，并检查 \(P\ll Q\)。
\item
  如果还想弄清原理：再追踪 Gibbs 不等式的等号条件、数据处理下可区分性的收缩，以及 JS 如何借助共同混合分布换取对称、有限的来源辨别尺度。
\end{itemize}

前一单元：\hyperref[ch:055]{``联合信息：一个变量能告诉我们另一个变量多少''}。

\subsection{本章篇目}

以下篇目按概念依赖排列；若从具体问题进入，也可以先打开最接近当前困惑的一篇，再沿篇内链接回补。

\begin{itemize}
\tightlist
\item \hyperref[sec:07-Information-Theory/03-Divergences/01]{``错误模型的代价：从交叉熵到 KL 散度''}；
\item \hyperref[sec:07-Information-Theory/03-Divergences/02]{``Gibbs 不等式与 KL 的方向性''}；
\item \hyperref[sec:07-Information-Theory/03-Divergences/03]{``Jensen--Shannon 散度与尺度选择''}。
\end{itemize}
