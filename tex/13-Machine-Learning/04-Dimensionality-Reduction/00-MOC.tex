一张包含上千列的数据表未必真的有上千个彼此独立的变化方向。多个传感器可能记录同一物理状态，多项消费指标可能共同反映少数偏好，图像的相邻像素也有强烈结构。降维所利用的第一性事实是：观测坐标很多，不等于生成数据的自由度很多。

这一单元先沿着``完整观测到不完整观测''的顺序展开，再转向弯曲结构：

\begin{enumerate}
\tightlist
\item
  PCA 从最大方差到最小重构误差：从相关特征带来的冗余出发，解释中心化、尺度选择、主方向和训练/测试隔离。
\item
  SVD 给出最佳低秩近似：把 PCA 背后的工具提升为一般矩阵近似，并讨论误差、谱隙和计算。
\item
  矩阵补全从缺失条目到低秩正则：进一步拿走大量观测，追问低秩结构何时足以恢复缺失项，何时无论算法多强都不可识别。
\item
  流形假设与非线性降维：当低维结构弯曲时，任何线性投影都会叠压不同层；Isomap 用测地距离替换欧氏距离，LLE 与 t-SNE 只保局部邻居，代价是近邻敏感、噪声短路与没有显式映射。
\item
  降维与升维的对偶：低维适合看清形态，高维适合精细交互；核方法与深度学习在高维计算，可视化与流形学习在低维理解。
\item
  因子分析与概率 PCA：把低秩结构写成带观测噪声的生成模型，分清共同因子与各坐标独有噪声，并说明旋转不可辨认性。
\item
  ICA 与 CCA：前者用独立性分离混合源，后者从两组变量寻找最大相关方向；不同结构假设决定``潜在方向''究竟能否被辨认。
\end{enumerate}

前三篇都在使用``低秩''，但任务不同：PCA 寻找样本变化的坐标系，截断 SVD 近似一个已知矩阵，矩阵补全则要从部分条目推断未知矩阵。混淆这三者，往往会把一个代数结论误当成统计保证。第四篇保留``自由度少''的信念，只放弃``全局线性''的假设：流形方法信任小尺度上的线性，不再相信大尺度上的线性。最后两篇回到生成与可辨认性：同样是线性潜变量，采用各向同性噪声、坐标特异噪声、非 Gaussian 独立性或跨视图相关性，会得到完全不同的方向。

\subsection{怎么读这一章}

主干路线是 PCA \(\rightarrow\) SVD \(\rightarrow\) 矩阵补全：先分清统计任务、代数工具和缺失数据识别问题。若关心可视化和局部几何，再读流形假设；若关心潜变量的现实含义，跳到因子分析、ICA 与 CCA，并把可辨认性放在解释之前。第一遍只需能说清``保留方差''、``保留距离''、``最小重构误差''和``恢复未观测条目''为何是不同目标，不必一次完成所有谱分解证明。

\subsection{本章篇目}

以下篇目按概念依赖排列；若从具体问题进入，也可以先打开最接近当前困惑的一篇，再沿篇内链接回补。

\begin{itemize}
\tightlist
\item \hyperref[sec:13-Machine-Learning/04-Dimensionality-Reduction/01]{``PCA 从最大方差到最小重构误差''}；
\item \hyperref[sec:13-Machine-Learning/04-Dimensionality-Reduction/02]{``SVD 给出最佳低秩近似''}；
\item \hyperref[sec:13-Machine-Learning/04-Dimensionality-Reduction/03]{``矩阵补全从缺失条目到低秩正则''}；
\item \hyperref[sec:13-Machine-Learning/04-Dimensionality-Reduction/04]{``流形假设与非线性降维''}；
\item \hyperref[sec:13-Machine-Learning/04-Dimensionality-Reduction/05]{``降维与升维的对偶：在合适的维度做合适的事''}；
\item \hyperref[sec:13-Machine-Learning/04-Dimensionality-Reduction/06]{``因子分析与概率 PCA：把低秩变成生成模型''}；
\item \hyperref[sec:13-Machine-Learning/04-Dimensionality-Reduction/07]{``ICA 与 CCA：选择不同的可辨认结构''}。
\end{itemize}
